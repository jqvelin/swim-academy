import { Map } from "@/widgets/Map";
import { Modal } from "@/widgets/Modal/ui/Modal";

const Page = () => {
    return (
        <Modal>
            <Map />
        </Modal>
    );
};

export default Page;
